# Dependances installees par Streamlit Community Cloud au deploiement.
#
# On liste les dependances EXPLICITEMENT plutot que d'installer le paquet
# local (`.[transform,dashboard]`). Raison : installer le paquet en copie
# le duplique dans site-packages, alors que `dashboard/app.py` ajoute deja
# `src/` au sys.path. Streamlit Cloud mettant l'environnement pip en cache,
# la copie site-packages se fige a une ancienne version et masque le code
# frais du depot - symptome typique : "Settings object has no attribute ...".
#
# Ici, `skytrace` n'existe qu'a un seul endroit : `src/`, toujours a jour
# avec le depot.

# --- Coeur (equivalent de [project.dependencies]) ---
httpx>=0.27
tenacity>=8.2
pyarrow>=16.0
duckdb>=1.0
pandas>=2.2
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Transformation (extra "transform") ---
dbt-core>=1.8
dbt-duckdb>=1.8

# --- Tableau de bord (extra "dashboard") ---
streamlit>=1.49
plotly>=5.22
pydeck>=0.9
